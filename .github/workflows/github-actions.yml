name: Mostrar Secret EC2_HOST

on:
  workflow_dispatch: # Permite ejecutar el workflow manualmente desde la interfaz de GitHub

jobs:
  mostrar_secret:
    runs-on: ubuntu-latest

    steps:
      - name: Imprimir el valor del secreto
        run: |
          echo "El valor del secreto EC2_HOST es: ${{ secrets.EC2_HOST }}"
